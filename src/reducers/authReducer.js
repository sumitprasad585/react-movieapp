const initialState = {
  apiKey: "634cfe434a3b3f747cb0e992c69543f7",
};

export default function authReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
